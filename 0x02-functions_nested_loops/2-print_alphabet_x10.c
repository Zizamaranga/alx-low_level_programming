#include "main.h"

/**
 * void - Entry point
 *
 * Return: Always 0 (Success)
 */

void print_alphabet_x10(void)i;
{
	int ch = 'a';
	while(ch <=  'z')
	{
		for (int ch = 0; ch <10; ch++);
		putchar(ch);
		return (0);
	}
}
